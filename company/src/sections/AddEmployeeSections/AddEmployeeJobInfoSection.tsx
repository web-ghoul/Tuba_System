import { Paper } from "@mui/material";
import Forms from "../../forms/Forms";

const AddEmployeeJobInfoSection = () => {
  return (
    <Paper className={`paper`}>
      <Forms type={"addEmployeeJobInfo"} />
    </Paper>
  );
};

export default AddEmployeeJobInfoSection;
