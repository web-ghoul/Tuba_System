import { Paper } from "@mui/material";
import Forms from "../../forms/Forms";

const AddEmployeePersonalInfoSection = () => {
  return (
    <Paper className={`paper`}>
      <Forms type={"addEmployeePersonInfo"} />
    </Paper>
  );
};

export default AddEmployeePersonalInfoSection;
