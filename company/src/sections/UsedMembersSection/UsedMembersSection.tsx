import { Box, Typography } from "@mui/material";

const UsedMembersSection = () => {
  return (
    <Box className={`paper`}>
      <Typography variant="h6" className={`!font-[700]`}>
        استخدام الاعضاء
      </Typography>
    </Box>
  );
};

export default UsedMembersSection;
