import AuthenticationSection from "../sections/AuthenticationSection/AuthenticationSection";

const UpdatePassword = () => {
  return <AuthenticationSection type={"updatePassword"} />;
};

export default UpdatePassword;
